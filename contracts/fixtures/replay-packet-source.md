# Replay packet fixture

This small, repo-contained fixture is used to prove ReplayPacket/v1 references
are content-bound without embedding raw replay input, hook input, hook output,
command output, transcript text, or bulky session payloads.
