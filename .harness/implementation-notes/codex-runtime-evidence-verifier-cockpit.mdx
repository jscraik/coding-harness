---
schema_version: implementation-notes/v1
title: Codex Runtime Evidence Verifier Cockpit Implementation Notes
goal: codex-runtime-evidence-verifier-cockpit
updated_at: 2026-05-26T09:25:00Z
status: present
---

# Codex Runtime Evidence Verifier Cockpit Implementation Notes

This MDX source is the version-controlled implementation-notes artifact for the goal board. The operator-facing browser view remains a local HTML projection while work is active.

## Deep Module Topology

<DeepModuleMap
  modules={[
    "src/lib/runtime",
    "src/lib/evidence",
    "src/lib/review-state",
    "src/lib/external-state",
    "src/lib/delivery-truth",
    "src/lib/root-hygiene",
    "src/lib/pr-closeout",
  ]}
/>

Runtime evidence work stays inside the existing deep modules instead of adding a parallel truth subsystem. `src/lib/runtime` owns Codex runtime packets, adapters, and runtime-card projection. `src/lib/delivery-truth` composes claim support from receipts, review state, external state, root hygiene, and PR closeout evidence.

## Current Slice Insertion Map

<InsertionPoint
	  slice="PU-012 Codex runtime evidence producer bridge"
	  branch="codex/jsc-363-runtime-evidence-cockpit-refresh"
	  files={[
	    "src/lib/runtime/codex-runtime-source-provenance.ts",
	    "src/lib/runtime/codex-runtime-source-provenance.test.ts",
	    "src/lib/runtime/codex-runtime-evidence-producer.ts",
	    "src/lib/runtime/codex-runtime-evidence-producer.test.ts",
	    ".harness/intent/codex-runtime-evidence-verifier-cockpit-pu-012-intent.json",
	  ]}
	/>

PU-012 adds the Harness-owned producer/import bridge for `codex-runtime-evidence/v1`. The bridge stays inside `src/lib/runtime`, validates packet shape before handoff, defaults unavailable trace, permission, external-state, and review-state facts to explicit unknown blocker classes, and preserves `/Users/jamiecraik/dev/codex` as read-only source evidence.

## Runtime Truth Surface

<RuntimeCardState
	  pr="305"
	  mergeable="draft/open"
	  merge_state="not merge-ready"
	  checks="focused local PU-012 gates passing; remote CI refresh pending after push"
	  review_threads="implementation reviewers running artifact-first"
	/>

PR #305 is the current draft lane for the full lifecycle implementation. Local PU-012 evidence is not a merge-readiness claim; remote CI, review state, and draft status remain separate external-state truths that must be refreshed after the next push.

## Blast Radius View

<BlastRadiusMap
	  changed_modules={[
	    "runtime source provenance",
	    "runtime evidence producer",
	    "PU-012 intent and review artifacts",
	  ]}
	/>

The slice is intentionally narrow: it does not alter runtime-card projection, `harness next --json`, delivery-truth verdict policy, review-state packets, external-state freshness, or Codex source code.

## Validation Coverage

<ValidatorCoverage
	  checks={[
	    "pnpm vitest run src/lib/runtime/codex-runtime-source-provenance.test.ts src/lib/runtime/codex-runtime-evidence-producer.test.ts",
	    "pnpm vitest run src/lib/runtime/codex-runtime-evidence.test.ts src/lib/runtime/runtime-evidence-adapter.test.ts src/lib/runtime/codex-runtime-evidence-producer.test.ts",
	    "./node_modules/.bin/biome check src/lib/runtime/codex-runtime-evidence-producer.ts src/lib/runtime/codex-runtime-evidence-producer.test.ts src/lib/runtime/codex-runtime-source-provenance.ts src/lib/runtime/codex-runtime-source-provenance.test.ts",
	    "pnpm typecheck",
	    "node scripts/check-code-size.mjs",
	    "node scripts/check-public-api-docs.mjs",
	    "pnpm run quality:self-affirming",
	    "bash scripts/check-related-tests.sh",
	  ]}
	/>

Validation is recorded in PR handoff only when the command was actually run and its pass, fail, or blocked result is known. The `pnpm run quality:*` aggregate wrapper stalled during this slice, so direct script invocations are the current evidence source for size, public API docs, and self-affirming tests.
