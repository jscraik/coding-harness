# Trust and Privacy

Public documentation is selected from repository-owned developer material.
The publication input excludes operator-private context, task snapshots, raw
sources, secrets, local paths, private provider data, and unsupported claims.

GitBook publication does not prove that tests passed, review was accepted, a
change can merge, or a deployment is healthy. Product builds and hosted CI do
not depend on GitBook or an operator's private context provider.
