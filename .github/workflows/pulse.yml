name: Pulse Automation

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pulse-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  pulse:
    name: pulse
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - name: Enable corepack
        run: corepack enable
      - name: Install dependencies
        run: pnpm install
      - name: Run pulse automation idempotently
        run: |
          mkdir -p artifacts/automation
          INPUT_FINGERPRINT="$(printf "%s" "${{ github.repository }}:${{ github.sha }}:pulse" | sha256sum | awk '{print $1}')"
          npx tsx src/cli.ts automation-run \
            --name pulse \
            --repo "${{ github.repository }}" \
            --head-sha "${{ github.sha }}" \
            --contract-version "1.2.0" \
            --input-fingerprint "${INPUT_FINGERPRINT}" \
            --artifacts-dir artifacts/automation \
            --json
      - name: Upload pulse artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pulse-automation-artifacts
          path: artifacts/automation/
          if-no-files-found: error
